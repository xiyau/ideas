extends ../layout   

block content
    if(idea)
        each ide in idea
            .card.card-body.mb-2
                h4=ide.title
                p=ide.details
                a(href='/ideas/edit/'+ ide._id) Edit
    else
        .card.card-body
            p !no ideas to list
